import {test} from '@jest/globals'
import {cm} from '../src/plastic'

// shows how the runner will run a javascript action with env / stdout protocol
test('test cm', () => {
  // TODO this will currently fail on cloud runner
  // cm('')
})
